<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.RegisterContacts">
        <activity
            android:name=".View.SignUp"
            android:exported="false"
            android:label="@string/register_contact" />
        <activity
            android:name=".View.RegisterContactsFragment"
            android:exported="false"
            android:label="Registrar novo contato" />
        <activity
            android:name=".View.ListContactsFragment"
            android:exported="false"/>
        <activity
            android:name=".View.Contacts"
            android:exported="false"
            android:label="@string/app_name" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>